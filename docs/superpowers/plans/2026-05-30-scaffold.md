# Scaffold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land a green-on-arrival Next.js 16 + React 19 + TS + Tailwind v4 + Vitest + ESLint + Prettier baseline with five empty section stubs (`#hero`, `#work`, `#journal`, `#about`, `#contact`).

**Architecture:** Strict step-1 scope. Configs first, then the empty `app/` shell, then the five section components built TDD (one per task), then CLAUDE.md updates, then a full verification pass. No design tokens, no fonts, no motion libraries, no MDX — those belong to later build-order steps.

**Tech Stack:** Next.js 16 (App Router), React 19, TypeScript strict, Tailwind CSS v4 (`@tailwindcss/postcss`), Vitest 4 + @testing-library/react + jsdom, ESLint flat config, Prettier.

**Working directory:** `/Users/romannguyen/Documents/Projects/roman-portfolio-website` — work directly on `main` per CLAUDE.md §7 (no worktree).

**Spec:** [docs/superpowers/specs/2026-05-30-scaffold-design.md](../specs/2026-05-30-scaffold-design.md)

---

## Pre-flight

These tasks assume the working tree currently contains only `CLAUDE.md`, `README.md`, `LICENSE`, `.gitignore`, `references/`, `.claude/`, and `docs/`. Every source file from HEAD has been wiped from the working tree but still exists in the index. The first task removes those stale index entries so we scaffold cleanly.

---

## Task 1: Clean stale index entries

Remove the old scaffold's tracked-but-deleted files from the index so `git status` is sane before we start writing new files.

**Files:**
- Delete from index: every `D` entry from `git status --short`
- Keep: `references/design-note.mb` (working tree has the updated version, just stage it)

- [ ] **Step 1: Stage all deletions and the modified design note**

Run:
```bash
git add -A
```

- [ ] **Step 2: Verify only deletions + the design-note modification are staged**

Run:
```bash
git status --short
```

Expected: lines starting with `D` for every deleted file plus one `M references/design-note.mb`. No `A` (added) lines because nothing new exists yet.

- [ ] **Step 3: Commit the cleanup**

```bash
git commit -m "$(cat <<'EOF'
chore: clear prior working-tree scaffold for fresh step 1

The prior scaffold + page loader + hero shader work is preserved in git
history (last commit da03bb5) but the working tree was wiped. Stage the
deletions and the updated design note so step 1 scaffolding starts from
a clean tree.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

Expected: commit succeeds; subsequent `git status` shows working tree clean except for `docs/superpowers/specs/2026-05-30-scaffold-design.md` (already committed) and any untracked dotfiles.

---

## Task 2: package.json + dependency install

Create the package manifest and install every dependency from spec §5 in one shot. The lockfile is generated by `npm install`.

**Files:**
- Create: `package.json`
- Generated: `package-lock.json`, `node_modules/`

- [ ] **Step 1: Write `package.json`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/package.json`:

```json
{
  "name": "roman-portfolio-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "next": "^16.2.6",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.2.6",
    "eslint-config-prettier": "^10.0.0",
    "jsdom": "^29.1.1",
    "prettier": "^3.3.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.7"
  }
}
```

- [ ] **Step 2: Install**

Run:
```bash
npm install
```

Expected: completes successfully. Peer-warning lines may print (Next 16 / React 19 / Vitest 4 still surface a few transitive peer notes); these are acceptable. Hard `npm ERR!` is not — if you see one, capture the message and stop before proceeding.

- [ ] **Step 3: Confirm install produced a lockfile and node_modules**

Run:
```bash
ls -la package-lock.json node_modules/.package-lock.json | head -2
```

Expected: both files present.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "$(cat <<'EOF'
chore: package.json + dependency install

Pin Next 16, React 19, Tailwind v4, Vitest 4, ESLint 9, Prettier 3 plus
React Testing Library and jsdom. No motion libs (gsap, lenis,
framer-motion) and no ogl yet — those land in later build-order steps.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: TypeScript, Next, and PostCSS configs

The three "structural" configs Next needs to boot.

**Files:**
- Create: `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`

- [ ] **Step 1: Write `tsconfig.json`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "ES2022"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 2: Write `next.config.ts`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/next.config.ts`:

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
```

- [ ] **Step 3: Write `postcss.config.mjs`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/postcss.config.mjs`:

```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

- [ ] **Step 4: Confirm typecheck runs (no source yet, so should be vacuously green)**

Run:
```bash
npm run typecheck
```

Expected: exit code 0. If TypeScript complains about `next-env.d.ts` being missing, that's OK — it gets generated on first `next build`. If the missing-file error is fatal, run `npx next telemetry status` (no-op command that triggers Next to generate `next-env.d.ts`), then re-run typecheck. The file is gitignored.

- [ ] **Step 5: Commit**

```bash
git add tsconfig.json next.config.ts postcss.config.mjs
git commit -m "$(cat <<'EOF'
chore: tsconfig, next.config, postcss.config

TypeScript strict mode with @/* path alias mapped to src/*.
Empty NextConfig (step 10 owns image/headers/etc).
PostCSS wired to @tailwindcss/postcss for Tailwind v4.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: ESLint flat config + Prettier

**Files:**
- Create: `eslint.config.mjs`, `.prettierrc.json`, `.prettierignore`
- Modify: `.gitignore` (extend with build artifacts)

- [ ] **Step 1: Write `eslint.config.mjs`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/eslint.config.mjs`:

```js
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";
import prettier from "eslint-config-prettier";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  prettier,
  globalIgnores([
    ".next/**",
    "out/**",
    "build/**",
    "coverage/**",
    "node_modules/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
```

- [ ] **Step 2: Write `.prettierrc.json`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/.prettierrc.json`:

```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2
}
```

- [ ] **Step 3: Write `.prettierignore`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/.prettierignore`:

```
.next
out
build
coverage
node_modules
package-lock.json
public
references/screenshots
references/recording.mp4
.playwright-mcp
```

- [ ] **Step 4: Extend `.gitignore`**

Read `/Users/romannguyen/Documents/Projects/roman-portfolio-website/.gitignore` first. Then ensure the following entries are present (add any missing ones at the end, do not duplicate):

```
# Build artifacts
.next/
out/
build/
coverage/

# Dependencies
node_modules/

# Next-generated
next-env.d.ts

# Test cache
.vitest-cache/

# Misc
.DS_Store
.playwright-mcp/
```

- [ ] **Step 5: Run lint to confirm config loads**

Run:
```bash
npm run lint
```

Expected: exit code 0 (no source files to lint, but the config must parse without error). If ESLint reports "0 files matched" or similar, that's still a pass.

- [ ] **Step 6: Run format:check on the configs we just wrote**

Run:
```bash
npm run format:check
```

Expected: exit code 0. If Prettier reports any formatting issues on the JSON/JS/TS configs we just wrote, fix them by running `npm run format` and re-running `format:check` until green.

- [ ] **Step 7: Commit**

```bash
git add eslint.config.mjs .prettierrc.json .prettierignore .gitignore
git commit -m "$(cat <<'EOF'
chore: ESLint flat config + Prettier

eslint-config-next (core-web-vitals + typescript) extended with
eslint-config-prettier so Prettier wins on formatting. Standard
Prettier rc with 100-char line, 2-space indent, trailing commas.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Vitest config + setup

**Files:**
- Create: `vitest.config.ts`, `vitest.setup.ts`

- [ ] **Step 1: Write `vitest.config.ts`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import path from "node:path";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    setupFiles: ["./vitest.setup.ts"],
    globals: true,
    css: false,
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});
```

- [ ] **Step 2: Write `vitest.setup.ts`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/vitest.setup.ts`:

```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 3: Run the test command to confirm Vitest boots**

Run:
```bash
npm run test
```

Expected: Vitest starts, reports "No test files found, exiting with code 0" (Vitest 4 exits clean on zero files when `passWithNoTests` is the default). If it instead exits non-zero, add `passWithNoTests: true` to the `test` block in `vitest.config.ts` and re-run.

- [ ] **Step 4: Commit**

```bash
git add vitest.config.ts vitest.setup.ts
git commit -m "$(cat <<'EOF'
chore: Vitest 4 + jsdom + RTL setup

jsdom env, globals enabled so tests don't import describe/it/expect,
@testing-library/jest-dom matchers extended via vitest.setup.ts, @/*
alias mirrors tsconfig.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `app/` shell — layout, page, globals.css

The minimal Next App Router shell. `page.tsx` is empty for now (no `<main>` content); the section stubs are wired in Task 12.

**Files:**
- Create: `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/globals.css`

- [ ] **Step 1: Write `src/app/globals.css`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/app/globals.css`:

```css
@import "tailwindcss";

@layer base {
  html,
  body {
    background: #000;
    color: #fff;
  }
}
```

- [ ] **Step 2: Write `src/app/layout.tsx`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/app/layout.tsx`:

```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Roman Nguyen — Portfolio",
  description: "Tokyo-born aesthetic. Saigon-built work.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="antialiased">
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 3: Write `src/app/page.tsx`**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/app/page.tsx`:

```tsx
export default function Home() {
  return <main />;
}
```

- [ ] **Step 4: Confirm typecheck still green**

Run:
```bash
npm run typecheck
```

Expected: exit code 0.

- [ ] **Step 5: Confirm build succeeds**

Run:
```bash
npm run build
```

Expected: build completes; output mentions `/` as a static route. No TypeScript errors. If `next-env.d.ts` complaints arose earlier, they are now gone because `next build` regenerates the file.

- [ ] **Step 6: Commit**

```bash
git add src/app/layout.tsx src/app/page.tsx src/app/globals.css
git commit -m "$(cat <<'EOF'
chore: app shell — RootLayout, empty Home, Tailwind import

Minimal layout (no fonts, no providers), an empty <main/> on Home, and
globals.css that imports Tailwind v4 plus a one-line black-bg reset to
avoid a white flash before step 2 lands design tokens.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Hero section stub (TDD)

The Hero is the only section using `<h1>` per CLAUDE.md "single `<h1>` on the hero". Tests assert the section renders as a region with the right accessible name and that the heading is an `<h1>`.

**Files:**
- Create: `src/sections/hero/index.tsx`
- Test: `src/sections/hero/index.test.tsx`

- [ ] **Step 1: Write the failing test**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/hero/index.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { Hero } from "./index";

describe("Hero section", () => {
  it("renders a region with id='hero' and accessible name 'Hero'", () => {
    render(<Hero />);
    const region = screen.getByRole("region", { name: "Hero" });
    expect(region).toHaveAttribute("id", "hero");
  });

  it("uses an <h1> for the heading", () => {
    render(<Hero />);
    const heading = screen.getByRole("heading", { name: "Hero", level: 1 });
    expect(heading).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run the test, confirm it fails**

Run:
```bash
npm run test -- src/sections/hero
```

Expected: FAIL with "Cannot find module './index'" or similar — the implementation doesn't exist yet.

- [ ] **Step 3: Write the minimal implementation**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/hero/index.tsx`:

```tsx
export function Hero() {
  return (
    <section id="hero" aria-labelledby="hero-heading">
      <h1 id="hero-heading">Hero</h1>
    </section>
  );
}
```

- [ ] **Step 4: Run the test, confirm it passes**

Run:
```bash
npm run test -- src/sections/hero
```

Expected: 2 passing tests.

- [ ] **Step 5: Commit**

```bash
git add src/sections/hero/
git commit -m "$(cat <<'EOF'
feat: Hero section stub with TDD coverage

Placeholder Hero section at #hero with an h1 heading.
Step 3 will replace this with the real shader-backed hero.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Work section stub (TDD)

**Files:**
- Create: `src/sections/work/index.tsx`
- Test: `src/sections/work/index.test.tsx`

- [ ] **Step 1: Write the failing test**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/work/index.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { Work } from "./index";

describe("Work section", () => {
  it("renders a region with id='work' and accessible name 'Work'", () => {
    render(<Work />);
    const region = screen.getByRole("region", { name: "Work" });
    expect(region).toHaveAttribute("id", "work");
  });

  it("uses an <h2> for the heading", () => {
    render(<Work />);
    const heading = screen.getByRole("heading", { name: "Work", level: 2 });
    expect(heading).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run the test, confirm it fails**

Run:
```bash
npm run test -- src/sections/work
```

Expected: FAIL — implementation missing.

- [ ] **Step 3: Write the minimal implementation**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/work/index.tsx`:

```tsx
export function Work() {
  return (
    <section id="work" aria-labelledby="work-heading">
      <h2 id="work-heading">Work</h2>
    </section>
  );
}
```

- [ ] **Step 4: Run the test, confirm it passes**

Run:
```bash
npm run test -- src/sections/work
```

Expected: 2 passing tests.

- [ ] **Step 5: Commit**

```bash
git add src/sections/work/
git commit -m "$(cat <<'EOF'
feat: Work section stub with TDD coverage

Placeholder Work section at #work. Step 6 will replace with the
MDX-backed project grid.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Journal section stub (TDD)

**Files:**
- Create: `src/sections/journal/index.tsx`
- Test: `src/sections/journal/index.test.tsx`

- [ ] **Step 1: Write the failing test**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/journal/index.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { Journal } from "./index";

describe("Journal section", () => {
  it("renders a region with id='journal' and accessible name 'Journal'", () => {
    render(<Journal />);
    const region = screen.getByRole("region", { name: "Journal" });
    expect(region).toHaveAttribute("id", "journal");
  });

  it("uses an <h2> for the heading", () => {
    render(<Journal />);
    const heading = screen.getByRole("heading", { name: "Journal", level: 2 });
    expect(heading).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run the test, confirm it fails**

Run:
```bash
npm run test -- src/sections/journal
```

Expected: FAIL — implementation missing.

- [ ] **Step 3: Write the minimal implementation**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/journal/index.tsx`:

```tsx
export function Journal() {
  return (
    <section id="journal" aria-labelledby="journal-heading">
      <h2 id="journal-heading">Journal</h2>
    </section>
  );
}
```

- [ ] **Step 4: Run the test, confirm it passes**

Run:
```bash
npm run test -- src/sections/journal
```

Expected: 2 passing tests.

- [ ] **Step 5: Commit**

```bash
git add src/sections/journal/
git commit -m "$(cat <<'EOF'
feat: Journal section stub with TDD coverage

Placeholder Journal section at #journal. Step 7 will replace with the
MDX-backed blog list.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: About section stub (TDD)

**Files:**
- Create: `src/sections/about/index.tsx`
- Test: `src/sections/about/index.test.tsx`

- [ ] **Step 1: Write the failing test**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/about/index.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { About } from "./index";

describe("About section", () => {
  it("renders a region with id='about' and accessible name 'About'", () => {
    render(<About />);
    const region = screen.getByRole("region", { name: "About" });
    expect(region).toHaveAttribute("id", "about");
  });

  it("uses an <h2> for the heading", () => {
    render(<About />);
    const heading = screen.getByRole("heading", { name: "About", level: 2 });
    expect(heading).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run the test, confirm it fails**

Run:
```bash
npm run test -- src/sections/about
```

Expected: FAIL — implementation missing.

- [ ] **Step 3: Write the minimal implementation**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/about/index.tsx`:

```tsx
export function About() {
  return (
    <section id="about" aria-labelledby="about-heading">
      <h2 id="about-heading">About</h2>
    </section>
  );
}
```

- [ ] **Step 4: Run the test, confirm it passes**

Run:
```bash
npm run test -- src/sections/about
```

Expected: 2 passing tests.

- [ ] **Step 5: Commit**

```bash
git add src/sections/about/
git commit -m "$(cat <<'EOF'
feat: About section stub with TDD coverage

Placeholder About section at #about. Step 8 will replace with the bio
+ capabilities + scroll-linked reveal.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: Contact section stub (TDD)

**Files:**
- Create: `src/sections/contact/index.tsx`
- Test: `src/sections/contact/index.test.tsx`

- [ ] **Step 1: Write the failing test**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/contact/index.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { Contact } from "./index";

describe("Contact section", () => {
  it("renders a region with id='contact' and accessible name 'Contact'", () => {
    render(<Contact />);
    const region = screen.getByRole("region", { name: "Contact" });
    expect(region).toHaveAttribute("id", "contact");
  });

  it("uses an <h2> for the heading", () => {
    render(<Contact />);
    const heading = screen.getByRole("heading", { name: "Contact", level: 2 });
    expect(heading).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run the test, confirm it fails**

Run:
```bash
npm run test -- src/sections/contact
```

Expected: FAIL — implementation missing.

- [ ] **Step 3: Write the minimal implementation**

Create `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/sections/contact/index.tsx`:

```tsx
export function Contact() {
  return (
    <section id="contact" aria-labelledby="contact-heading">
      <h2 id="contact-heading">Contact</h2>
    </section>
  );
}
```

- [ ] **Step 4: Run the test, confirm it passes**

Run:
```bash
npm run test -- src/sections/contact
```

Expected: 2 passing tests.

- [ ] **Step 5: Commit**

```bash
git add src/sections/contact/
git commit -m "$(cat <<'EOF'
feat: Contact section stub with TDD coverage

Placeholder Contact section at #contact. Step 9 will replace with the
email + socials + optional form.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: Wire sections into Home

Replace the empty `<main />` with all five sections in build order.

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Rewrite `src/app/page.tsx`**

Replace the contents of `/Users/romannguyen/Documents/Projects/roman-portfolio-website/src/app/page.tsx` with:

```tsx
import { Hero } from "@/sections/hero";
import { Work } from "@/sections/work";
import { Journal } from "@/sections/journal";
import { About } from "@/sections/about";
import { Contact } from "@/sections/contact";

export default function Home() {
  return (
    <main>
      <Hero />
      <Work />
      <Journal />
      <About />
      <Contact />
    </main>
  );
}
```

- [ ] **Step 2: Run the full test suite**

Run:
```bash
npm run test
```

Expected: 10 passing tests across 5 test files (2 per section).

- [ ] **Step 3: Run typecheck**

Run:
```bash
npm run typecheck
```

Expected: exit code 0.

- [ ] **Step 4: Run lint**

Run:
```bash
npm run lint
```

Expected: exit code 0.

- [ ] **Step 5: Run format:check**

Run:
```bash
npm run format:check
```

Expected: exit code 0. If anything is off, run `npm run format` and re-run `format:check` until green.

- [ ] **Step 6: Run build**

Run:
```bash
npm run build
```

Expected: build succeeds; output lists `/` as static route.

- [ ] **Step 7: Commit**

```bash
git add src/app/page.tsx
git commit -m "$(cat <<'EOF'
feat: wire the five section stubs into Home

Home renders <Hero/>, <Work/>, <Journal/>, <About/>, <Contact/> in
build order. All ten section tests pass; lint, typecheck, format, and
build are green.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 13: CLAUDE.md updates

Bring the always-loaded project instructions in line with what we actually scaffolded.

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Read the current CLAUDE.md**

Read `/Users/romannguyen/Documents/Projects/roman-portfolio-website/CLAUDE.md` so you can match the exact surrounding text for the edits.

- [ ] **Step 2: Update §2 (tech stack) — Next 15 → Next 16**

Replace the bullet:

```
- **Framework:** Next.js 15 (App Router) + React 19
```

with:

```
- **Framework:** Next.js 16 (App Router) + React 19
```

- [ ] **Step 3: Tick §5 step 1 checkbox**

Replace:

```
- [ ] **1. Scaffold** — Next.js 15 + TS + Tailwind v4 + Vitest + ESLint/Prettier; empty section stubs; CI-free local-only baseline
```

with:

```
- [x] **1. Scaffold** — Next.js 16 + TS + Tailwind v4 + Vitest + ESLint/Prettier; empty section stubs; CI-free local-only baseline
```

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
docs: mark step 1 (scaffold) complete; update Next version to 16

Tech stack bullet and step-1 checkbox now reflect what's actually on
main.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 14: Full verification gate

Run every check from spec §7 in order. This is verification only — no commits unless something fails and needs a fix.

- [ ] **Step 1: Reinstall to confirm a cold install is clean**

Run:
```bash
rm -rf node_modules
npm install
```

Expected: completes without `npm ERR!`. Peer warnings are acceptable.

- [ ] **Step 2: Typecheck**

Run:
```bash
npm run typecheck
```

Expected: exit code 0.

- [ ] **Step 3: Lint**

Run:
```bash
npm run lint
```

Expected: exit code 0.

- [ ] **Step 4: Format check**

Run:
```bash
npm run format:check
```

Expected: exit code 0.

- [ ] **Step 5: Tests**

Run:
```bash
npm run test
```

Expected: 10 passing tests across 5 files.

- [ ] **Step 6: Build**

Run:
```bash
npm run build
```

Expected: succeeds.

- [ ] **Step 7: Dev server + browser check**

Run in background:
```bash
npm run dev
```

Wait until the terminal shows "Ready in ...". Then in your browser, open http://localhost:3000.

Confirm visually:
- Page loads on black background.
- Five headings stack down the page in order: "Hero", "Work", "Journal", "About", "Contact".
- DevTools Elements panel shows `<section id="hero">`, `<section id="work">`, etc.
- DevTools Console has zero errors and zero React hydration warnings.

Stop the dev server (Ctrl+C) when verification is complete.

- [ ] **Step 8: Push to origin/main**

Run:
```bash
git push origin main
```

Expected: succeeds.

---

## Out-of-scope reminders

If during execution you find yourself wanting to add any of the following, **stop and don't do it** — these are owned by later build-order steps:

- Design tokens / palette / type scale (step 2)
- `next/font` or Fontshare imports (step 2)
- Page loader (step 3 or 4)
- Lenis / GSAP / framer-motion / ogl (steps 3 & 5)
- Custom shader / WebGL fallback (step 3)
- MDX deps or content pipeline (steps 6/7)
- Nav header (step 4)
- public/ assets, OG images, favicon SVGs (step 10)
- CI workflow (CLAUDE.md says "CI-free local-only baseline")

If you discover during execution that something in this list is actually required to make a test pass, stop and raise it instead of expanding scope silently.

## Known omissions vs. spec §3

The spec's file tree listed `src/app/favicon.ico`. This plan omits it — browsers will fall back to a default icon, and asset work is deferred to step 10. No test or command checks for the favicon's presence.

## Self-review notes (for the plan author, leave intact)

- **Spec coverage:** §2 stack → Task 2; §3 file structure → Tasks 2–12; §4 component contracts → Tasks 7–11; §5 toolchain configs → Tasks 2–5; §6 CLAUDE.md updates → Task 13; §7 verification gate → Task 14; §8 out-of-scope → reinforced in "Out-of-scope reminders" above.
- **Placeholder scan:** every code step has the full code; every command step has the exact command and expected output. No "TBD" or "TODO" anywhere.
- **Type consistency:** Section components all export a named function matching their section name (`Hero`, `Work`, `Journal`, `About`, `Contact`). `page.tsx` imports them via the `@/sections/<name>` alias. `id` and `aria-labelledby` slugs match between component and test in every section.
